# py_requirements.vm - generated requirements.txt for Flask project
# Core
Flask>=2.3.0
Flask-Login>=0.6.0
Flask-SQLAlchemy>=3.0.0
Flask-Migrate>=4.0.0
Flask-Mail>=0.9.0
Flask-Babel>=3.0.0
flask-cors>=4.0.0

# Config and env
python-dotenv>=1.0.0
dynaconf>=3.2.0

# Database
SQLAlchemy>=2.0.0
pymysql>=1.1.0
# PostgreSQL driver (used when DB_DRIVER=postgresql or DATABASE_URI uses postgresql+psycopg)
psycopg[binary]>=3.1.0

# Serialization and HTTP
jsonpickle>=3.0.0
requests>=2.28.0
openpyxl>=3.1.0

# Security: password hashing (used by utils.encrypt)
bcrypt>=4.0.0
tomli>=1.2.3; python_version < "3.11"

# Production WSGI server
gunicorn>=21.0.0

# Server-side session (Flask-Session; avoids cookie size limit for large session data)
Flask-Session>=0.8.0
